# JSON Parser (PHP)

A simple hand-written JSON parser built in PHP.  
It tokenizes input and parses objects into PHP arrays using recursive descent parsing.

## Run

```bash
php parser.php input.json